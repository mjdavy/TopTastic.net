<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TopTastic.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:TopTastic.ViewModel"
    x:Class="TopTastic.View.Search"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400" Width="300" Height="200">

    <Grid>
        <Grid.DataContext>
            <ViewModel:SearchViewModel/>
        </Grid.DataContext>
        <Button x:Name="CancelButton" Content="Cancel" HorizontalAlignment="Right" Margin="0,0,10,10" VerticalAlignment="Bottom"/>
        <Button x:Name="GoButton" Content="Go" HorizontalAlignment="Right" Margin="0,0,80,10" VerticalAlignment="Bottom"/>
        <TextBox x:Name="textBox" Margin="10,34,10,0" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" d:LayoutOverrides="VerticalAlignment, LeftPosition, RightPosition" PlaceholderText="Artist"/>
        <TextBox x:Name="textBox1" Margin="10,71,10,97" TextWrapping="Wrap" Text="TextBox" d:LayoutOverrides="VerticalAlignment, Height, LeftPosition, RightPosition, TopPosition, BottomPosition" PlaceholderText="Title"/>
    </Grid>
</UserControl>
